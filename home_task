HW
1. utils.py - make TRIP of slashes from path on 26 and 33 strings
2. templates.html - make like {% extends 'base.html' %}
3. index.py - cookie = cookies.SimpleCookie(self.headers.get('Cookie')  # move to utils file
4. Add in ACTION a new action - "edit" to open task in a new window
it will require join functionality from CLOSE (actions.close_task) and LOGIN (actions.users) methods - 
to join redirects and POST - ask Oleg in case of problems
3. логирование - убрать из консоли логи - Убрал отображение списка задач
3.5 - сделать Logout- как это сделать??? если вышел, то его надо перекидывать на страницу авторизации и убивать сессию-удалять токен
4. Начать переделывать actions для задач под архитектуру текущую 
5. Если пользователь авторизован, то показать страницу сон писком задач. Если нет, то не отображать ни на одной странице ссылку на это
6. если БД с сессиями пустая, то не происходит автоматического добавления новой сессии в БД



Notes (Oleg):
1. Imports inside the project
2. %s

To Do:
1. Rewrite DB - link ID and user_name from tables "tasks" and "users"



ohmyz.sh - about plugin



1.Expanded data seeding for users sessions (migrations -> def data_seeding_sessions)
2.Added a method for getting a list of users_id (ORM -> SessionsWork -> get_list_users_id)
3.Added a method for deleting users_id session (ORM -> SessionsWork -> delete_session)


